extern def is_odd(n):
    if n % 2 == 0:
        return 0;
    else:
        return 1;
    endif
enddef

extern def is_even(n):
    return not is_odd(n);
enddef

extern def mul8(n):
    return 100 - 100 +
           n * 2 * 2 * 2;
enddef
